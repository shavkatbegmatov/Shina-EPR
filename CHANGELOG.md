# O'zgarishlar tarixi (Changelog)

Format [Keep a Changelog](https://keepachangelog.com/) asosida; versiyalar [SemVer](https://semver.org/).
`release.yml` shu fayldagi `## [X.Y.Z]` bo'limidan reliz izohini oladi (`v*` teg push'da).

## [Unreleased]
### Added
- **Kassa smenasi va Z-hisobot.** Kassir kun oxirida qancha pul topshirishi kerakligini tizim bilmasdi — boshlang'ich kassa qoldig'i, naqd tushum va sanab topshirilgan pul hech qayerda solishtirilmasdi, ya'ni kamomad sezilmay qolardi. Endi Smenalar sahifasida smena ochiladi (mayda pul bilan), savdolar unga bog'lanadi, yopishda esa sanalgan pul kutilgan summa bilan solishtirilib Z-hisobot chiqadi (chop etsa bo'ladi). Naqd hisobida faqat `CASH` savdolarning **to'langan** qismi ishtirok etadi — qarzga sotilgan qism kassaga tushmagan; bekor qilingan savdolar chiqarib tashlanadi. Yopilgandagi kutilgan summa va farq **saqlanadi**, shuning uchun keyinchalik savdo bekor qilinsa ham kamomad yo'qolmaydi. Ochiq smena bo'lmasa savdo **to'silmaydi** (mavjud POS oqimi buzilmasin) — bunday savdolar Z-hisobotga tushmasligi sahifada aytiladi.
- **Kassa cheki chop etish.** Ilgari chek umuman yo'q edi (`window.print` butun kod bazasida topilmasdi) — kassa cheksiz ishlardi. Endi POS'da savdo yakunlangach modal chek bosqichiga o'tadi (hisob-faktura raqami, summa, "Chek chop etish"), savdo tafsilotlari sahifasida esa qayta chop etish tugmasi bor. Chek 80mm termal printer uchun `@media print` bilan tayyorlangan — alohida kutubxona yoki drayver kerak emas. Chek **avtomatik chiqmaydi**: printeri yo'q do'konda har savdoda brauzer dialogi ochilib turishi noqulay. Do'kon nomi/telefoni/manzili va pastki matn Sozlamalar → Chek bo'limida (V31; bo'sh qoldirilgan qator chekda ko'rinmaydi).
- **Shina o'lchami bo'yicha server qidiruvi.** `/v1/catalog` endi `width`/`profile`/`diameter` parametrlarini qabul qiladi, va qidiruv satridan o'lchamni o'zi ajratib oladi ("205/55R16", "205/55 R16", "205/55-16", "r17", "michelin 205/55r16"). Ilgari qidiruv faqat nom/SKU/brend bo'yicha LIKE qilardi — dev bazasida `205/55R16` so'rovi **0 ta natija** berardi, holbuki mos mahsulot bor edi (o'lcham alohida ustunlarda saqlanadi). Vitrina ham endi o'lcham va qidiruvni serverga uzatadi: ilgari ular yuklab olingan birinchi 200 mahsulot ichida filtrlanardi, ya'ni katalog o'sganda qolganlari umuman topilmasdi. O'lcham tanlagichi ro'yxatlari `/v1/catalog/facets` dan — butun katalog bo'yicha, 200 talik namunadan emas.

### Security
- **Ommaviy seed parollari zararsizlantirildi.** `V2`/`V3` (`admin/admin123`, `seller/seller123`) va `V6` (test mijoz, PIN `1234`) `db/migration` ichida bo'lgani uchun production bazasiga ham tushardi, hash'lar esa repoda ochiq. `V29` test mijoz portalini o'chiradi; `DefaultCredentialGuard` (dev'dan tashqari) ishga tushishda parol hali standart ekanini `matches()` bilan tekshirib almashtiradi. Yangi parol `ADMIN_INITIAL_PASSWORD` dan olinadi, bo'lmasa tasodifiy generatsiya qilinib logga bir marta yoziladi (akkaunt bloklanmaydi — egasi qulflanib qolmasligi uchun).
- **To'lov webhook'lari fail-closed qilindi.** Click `complete` `enabled`ni tekshirmasdi va `secretKey` defaulti bo'sh edi — bo'sh kalitda MD5 imzo faqat ommaviy maydonlardan hisoblanardi, ya'ni begona odam buyurtmani to'lamasdan `PAID` qila olardi. Endi o'chiq integratsiya/bo'sh kalit rad etiladi, `complete`ga summa tekshiruvi qo'shildi, takroriy chaqiruv idempotent, imzo doimiy-vaqtda solishtiriladi. Payme'ga ham `enabled` tekshiruvi qo'shildi va istisno matni endi provayderga qaytarilmaydi.
- **Qarz va dashboard endpointlariga avtorizatsiya qo'shildi.** `DebtController`ning 13 endpointidan faqat `/export` himoyalangan edi — jumladan `POST /{id}/pay` va `/pay-full` moliyaviy yozuvlari. `DASHBOARD_VIEW` ham backend'da tekshirilmasdi. `ControllerAuthorizationCoverageTest` bundan keyin annotatsiyasiz endpointda CI'ni yiqitadi.
- **JWT default kaliti olib tashlandi.** `application.yml`dagi `${JWT_SECRET:mS1mNCt...}` fail-open edi. Endi kalit berilmasa ilova ishga tushmaydi; `JwtTokenProvider` bo'sh, qisqa (<32 bayt), noto'g'ri base64 va eski sizib ketgan kalitni aniq xabar bilan rad etadi. Dev default `application-dev.yml`ga ko'chirildi.
- CI backend endi `mvnw clean test` bajaradi (ilgari `compile -DskipTests` edi) — yuqoridagi qorovul testlari aks holda hech qachon ishlamasdi.
- **Bildirishnomalarda egalik tekshiruvi (IDOR).** `markAsRead`/`deleteNotification` faqat mavjudlikni tekshirardi — istalgan xodim id'ni sinab boshqasining bildirishnomalarini o'chira olardi. Endi `user.id = :userId OR user IS NULL` sharti bilan scoping qilinadi (global bildirishnomalar xatti-harakati o'zgarmadi).

- **Mijoz login'ida enumeration yopildi.** Metod oltita farqli xabar qaytarardi ("portal yoqilmagan", "hisob faol emas", "PIN o'rnatilmagan", "bloklangan, N daqiqa", "qolgan urinishlar: N") va faqat bittasi raqam topilmaganda chiqardi — ya'ni javobning o'zi telefon raqam do'kon mijozimi yoki yo'qmi degan savolga aniq javob berardi. Endi akkaunt holati haqidagi aniq xabar **faqat PIN to'g'ri bo'lgandan keyin** beriladi; aks holda yagona umumiy xabar (qulflash siyosati bilan, shunda haqiqiy foydalanuvchi ogohlantirishni yo'qotmaydi). Raqam topilmaganda ham bcrypt bajariladi — javob vaqti enumeration kanaliga aylanmasin.
- **Login'da IP bo'yicha throttle.** Himoya faqat foydalanuvchi nomi bo'yicha edi (5 urinish / 30 daqiqa), ya'ni credential stuffing to'silmasdi: bitta parolni minglab turli login'ga sinab ko'rsa, hech bir akkaunt chegaraga yetmasdi. Endi xodim va mijoz login'lariga IP bo'yicha chegara (30 muvaffaqiyatsiz urinish / 15 daqiqa) qo'shildi. Faqat **muvaffaqiyatsiz** urinishlar hisoblanadi va muvaffaqiyatli kirish byudjetni tozalaydi — bitta ofis IP'si ortidagi xodimlar bloklanmaydi.
- **`X-Forwarded-For` spoofing yopildi.** `forward-headers-strategy: framework` `getRemoteAddr()` ni XFF ning **birinchi** (mijoz yuborgan) qiymatiga o'rnatardi — hujumchi har so'rovda soxta IP yozib throttle'ni butunlay chetlab o'ta olardi. `native` (Tomcat RemoteIpValve) ga o'tildi: u XFF ni o'ngdan chapga yurib ishonchli proksilarni o'tkazib yuboradi. Xatti-harakat `ClientIpTest`da o'lchangan.
- **`ResponseStatusException` statusi saqlanadi.** `@ExceptionHandler(Exception.class)` catch-all uni ham ushlab, **har qanday statusni 500 ga aylantirardi** — buyurtma endpointidagi mavjud rate-limit javobi (429) mijozga "Ichki server xatosi" bo'lib borardi.

### Performance
- **Eksportda chegarasiz `maxRecords` yopildi.** 13 ta eksport endpointi `maxRecords` ni to'g'ridan-to'g'ri `PageRequest`ga uzatardi, yuqori chegarasiz — `?maxRecords=100000000` butun jadvalni xotiraga yuklab, undan Excel/PDF quriladi, ya'ni bitta so'rov ilovani OOM bilan yiqita olardi. Endi `ExportSupport` maksimal 50 000 qatorni talab qiladi (oshsa aniq 400, jimgina kesish emas), nol/manfiy qiymat ham 400 beradi (ilgari 500 edi). Chegara ichida bo'lsa ham natija to'liq bo'lmasa, `X-Export-Truncated` sarlavhasi va WARN log bildiradi.
- **Mijoz portali statistikasi.** `getDashboardStats` savdolar **sonini** olish uchun `Pageable.unpaged()` bilan mijozning barcha savdolarini yuklardi; endi `countByCustomerId`.
- **N+1 so'rovlar bartaraf etildi.** Ro'yxat endpointlarining barcha response mapper'lari LAZY assotsiatsiyalarga tegardi (`SaleResponse` → customer/createdBy/items/item.product, `DebtResponse` → customer/sale, `StockMovementResponse` → product/supplier/createdBy, `ProductResponse`/`CatalogProductResponse` → brand/category). Bundan tashqari `AuditEntityListener` har `@PostLoad`da `toAuditMap()` chaqiradi, u esa `items.size()` bilan kolleksiyalarni ishga tushirardi. Global `hibernate.default_batch_fetch_size: 50` va issiq ro'yxat so'rovlariga `@EntityGraph` qo'shildi. **O'lchangan: 20 qatorlik savdo sahifasi 104 → 6 ta so'rov**; so'rovlar soni endi qatorlar soniga qarab o'smaydi. Kolleksiyalar `@EntityGraph`ga ATAYLAB qo'shilmadi — `@OneToMany` + `Pageable` Hibernate'ni xotirada sahifalashga majbur qiladi (HHH000104).

### Fixed
- **Jimgina yutilgan xatolar endi ko'rinadi.** 84 ta `catch → console.error` dan faqat 7 tasi foydalanuvchiga xabar berardi. **Mutatsiyalarda** (19 joy) xodim "Saqlash" bosib, so'rov yiqilsa hech qanday xabar chiqmasdi va u saqlandi deb o'ylardi — endi `toast.error(getApiErrorMessage(...))`. **Ro'yxat yuklashlarida** (9 sahifa) xato bo'sh jadval bo'lib ko'rinardi ("Ma'lumot topilmadi") — operator zaxira yo'q deb o'ylashi mumkin edi; endi `DataTable`ning xato paneli va "Qayta urinish" tugmasi. `dataTableErrorCoverage.test.ts` yangi jadval `error` propisiz qo'shilsa CI'ni yiqitadi.
- **Oq ekran o'rniga tiklanadigan xato holati (`ErrorBoundary`).** Kod bazasida bitta ham xato chegarasi yo'q edi (`ErrorBoundary`/`componentDidCatch`/`errorElement` — 0 ta), ya'ni bitta komponentdagi ushlanmagan render xatosi butun daraxtni yechib, foydalanuvchiga bo'sh oq sahifa berardi — na xabar, na tiklanish yo'li. Chegara uchala layout'da (`MainLayout`, `PortalLayout`, `ShopLayout`) `<Outlet />` atrofida: sahifa yiqilsa ham sidebar/header/navigatsiya saqlanadi va boshqa bo'limga o'tish mumkin. Layout'ning o'zi yiqilgan holat (lazy chunk yuklanmasligi) uchun router darajasida `errorElement`. Chegara marshrut almashganda avtomatik tiklanadi (`resetKeys`); xato tafsilotlari faqat DEV'da ko'rsatiladi.
- **Zaxira manfiyga tushishi va "chiqim" orqali zaxira oshishi.** `StockAdjustmentRequest.quantity` da chegara yo'q edi; `OUT` qorovuli `quantity > previousStock` bo'lgani uchun `-100` undan o'tib ketib zaxirani 100 taga **oshirardi**, `ADJUSTMENT` esa zaxirani to'g'ridan-to'g'ri manfiy qilardi. Endi DTO'da `@Min(0)`, servisda harakat turiga xos qoida. `PurchaseService.completeReturn` ham joriy zaxirani tekshiradi (ilgari sotilgan moldan qaytarish zaxirani manfiyga tushirardi).
- **Audit listener'ning xotira sizishi va audit yozuvining yo'qolishi.** Dastlabki holat `static ConcurrentHashMap`da saqlanib, o'qilgan-u yozilmagan har bir entity (ro'yxatlar, hisobotlar, 10 000 qatorlik eksport) xotirada abadiy qolardi; global kalitlash tufayli konkurent yangilashda audit yozuvi jimgina tushmay qolardi. Endi `AuditStateContext` — ThreadLocal, tranzaksiya tugashida avtomatik bo'shatiladi, `@PreUpdate` baseline'ni yemaydi.
- **Hujjat raqamlari poygasi.** `invoice_number`/`order_number`/`return_number` "MAX(...) + 1" bilan yasalardi: ikki kassir bir vaqtda savdo qilsa bir xil raqam olib, UNIQUE cheklov tufayli yutqazganning **savdosi yo'qolardi**. Endi `document_sequences` jadvali va atomik `INSERT ... ON CONFLICT DO UPDATE` (V30, mavjud ma'lumotdan seed qilinadi).
- **Vitrina backend yiqilganda soxta mahsulot ko'rsatmaydi.** `useCatalog` xatoni yutib `DEMO_PRODUCTS`ga tushardi — productionda ham. Demo massiv haqiqiy brend nomlari va narxlar bilan to'lgan ("Michelin Primacy 4", 1 200 000 so'm), shuning uchun bitta tarmoq uzilishi ommaviy do'konni ogohlantirishsiz xayoliy katalogga o'tkazardi va mijoz mavjud bo'lmagan mahsulotga buyurtma bera olardi. Endi fallback faqat `import.meta.env.DEV`da; productionda hooklar `isError` qaytaradi, Bosh sahifa/Katalog/Mahsulot sahifalari esa qayta urinish tugmasi bilan xato holatini ko'rsatadi. Qo'shimcha: `retry: false` → `retry: 2` (o'tkinchi uzilish endi yopiladi), mahsulot sahifasi yuklanish paytida "topilmadi" deb yolg'on aytmaydi.
- **Konkurentlik istisnolari endi 409 qaytaradi.** `ObjectOptimisticLockingFailureException` va `DataIntegrityViolationException` uchun handler yo'q edi — ikkalasi ham "Ichki server xatosi" (500) bo'lardi. Bu ayniqsa vitrinada sezilardi: ikki xaridor bitta shinani bir vaqtda buyurtma qilsa `@Version` oversell'ni to'g'ri to'sardi, lekin mijoz 500 ko'rib buyurtma yo'qolardi.

### Added
- Katalog xususiyatlar shajarasi (WB-uslub): kategoriya daraxti (icon/tartib, subtree filtri) + atributlar (SELECT/MULTI_SELECT/NUMBER/BOOLEAN/TEXT, variantlar) + kategoriya-atribut merosi + mahsulot atribut qiymatlari (V26). Admin: Kategoriyalar/Xususiyatlar/Brendlar sahifalari, mahsulot formasida dinamik xususiyatlar (majburiylik nazorati), guruhlangan sidebar. Ommaviy API: `GET /v1/catalog/facets` (kategoriya daraxti + narx diapazoni + variant hisoblagichli filtrlar) va `GET /v1/catalog?attrs=attrId:optId,...;...&priceMin&priceMax&inStock` filtrlash.
- Vitrina katalogida WB-uslub filtr paneli: kategoriya daraxti (subtree hisoblagichlar), narx diapazoni, "faqat sotuvda borlar", atribut facetlari — server tomonda filtrlash (URL'da ulashiladigan `cat/pmin/pmax/stock/attrs` paramlar), mobilda drawer, backend yo'q bo'lsa demo-rejimga graceful qaytish. PDP xususiyatlar jadvalida kategoriya atributlari.
- Universal magazin: kategoriya "forma shabloni" (V27, `template`, bola kategoriyalarga meros) — shina o'lcham maydonlari (eni/profil/diametr, yuk/tezlik, mavsum) faqat TIRE shablonli kategoriyalarda; boshqa mahsulotlar (aksessuar, disk...) faqat atributlar bilan. Admin ro'yxati/filtri/ustunlari, vitrina (o'lcham qidiruvchisi, mavsum), POS, solishtirish sahifasi tanlangan kategoriyaga moslashadi; solishtirishda atribut qatorlari. Demo: aksessuar mahsulotlar.
- Zaxira/tannarx yagona manbasi: mahsulot formasidan Miqdor va Kelish narxi olib tashlandi — zaxira faqat Ombor (kirim/chiqim/tuzatish), Xaridlar va Savdo orqali o'zgaradi; tannarx oxirgi kirim/xarid narxidan avtomatik yoziladi. Ombor kirimida narx ta'minotchisiz ham kiritiladi va joriy tannarx taklif qilinadi. Forma seksiyalarga bo'lindi (Asosiy / Shina o'lchamlari / Xususiyatlar / Narx va zaxira / Tavsif-rasm), tahrirda joriy zaxira read-only. Muhim fix: mahsulot tahririda zaxira 0 bo'lib ketish xavfi bartaraf etildi.

### Changed
- Formalar dizayni (loyiha bo'ylab): "filled field" uslubi — barcha input/select/textarea va maxsus komponentlar (Select, SearchInput, NumberInput, CurrencyInput) to'ldirilgan fon bilan aniq ko'rinadi, fokusda toza fonga ko'tarilib primary halqa oladi; `.form-card` guruh-kartalari, aksent belgili seksiya sarlavhalari, mahsulot modalida sticky sarlavha/amallar paneli.
- Storefront mijoz akkaunti (telefon+PIN, buyurtma tarixi backend'dan) — Faza 6.
- Mijoz kabineti birlashuvi: `/hisob` (yagona login `/kirish`, ERP xaridlar + do'kon buyurtmalari) — D1.
- Buyurtma xabarnoma poydevori (email SMTP + SMS-stub, config-gated) — F7.
- Mahsulot rasm yuklash (lokal-FS storage + Coolify volume) — B4/C5.
- Storefront SEO: OG/Twitter meta + og-cover + sitemap/robots — B3.
- Coolify deploy: docker-compose + Dockerfile/nginx + `application-prod.yml` + CI/CD (ghcr + webhook) + `DEPLOY.md`.

### Changed
- Routing: do'kon ildizda (`/`), ERP `/admin`da — B2.

### Notes
- Jonli to'lov (Payme/Click), SMS provider (Eskiz) impl va deploy kreditsiallari — A-guruhi (`DEPLOY.md`).
